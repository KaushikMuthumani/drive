// Schedule is now managed through Batches → Sessions.
// This file is kept only so old imports don't break during migration.
// The admin/schedule page redirects to /admin/batches.
export default function SchedulePage() { return null }
